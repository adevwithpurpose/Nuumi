---
phase: 02-homepage-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [assets/nuumi-base.css, snippets/nuumi-icons.liquid, snippets/nuumi-rating-stars.liquid, cypress/e2e/homepage.cy.js, package.json]
autonomous: true
requirements: [HOME-01, HOME-02, HOME-03, HOME-04, HOME-05, HOME-06, HOME-07, HOME-08, HOME-09]
user_setup: []

must_haves:
  truths:
    - "nuumi-base.css defines the aggressive gold button gradient and brand tokens"
    - "nuumi-icons.liquid contains all brand-specific SVGs (paw, science, shield, etc.)"
    - "Cypress is installed and a basic homepage test exists"
  artifacts:
    - path: "assets/nuumi-base.css"
      provides: "Global brand tokens and .nuumi-button--gold styles"
    - path: "snippets/nuumi-icons.liquid"
      provides: "Central SVG icon registry"
    - path: "cypress/e2e/homepage.cy.js"
      provides: "Initial E2E test suite for homepage sections"
  key_links:
    - from: "nuumi-base.css"
      to: "Shopify Theme Editor"
      via: "CSS variables (tokens)"
      pattern: "--nuumi-gold"
---

<objective>
Establish the global styling foundation and shared snippets for the Phase 2 homepage sections. This includes the 'aggressive gold' CTA button and the necessary brand iconography to ensure visual consistency across all modular sections.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-homepage-sections/02-CONTEXT.md
@.planning/phases/02-homepage-sections/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>task 1: update brand tokens and gold CTA styling</name>
  <files>assets/nuumi-base.css</files>
  <action>
    Refine `assets/nuumi-base.css` to include the 'premium shine' gold CTA styling.
    - Update `--nuumi-gold` to a vibrant Amber (`#F59E0B`).
    - Implement `.nuumi-button--gold` class with a linear gradient (135deg, #F59E0B 0%, #D97706 100%) and a prominent shadow.
    - Add the 'shine' animation effect as described in the research (`nuumi-shine`).
    - Ensure font tokens (DM Serif Display, Plus Jakarta Sans) are correctly mapped to CSS variables.
  </action>
  <verify>
    <automated>grep -q ".nuumi-button--gold" assets/nuumi-base.css</automated>
  </verify>
  <done>nuumi-base.css updated with brand tokens and gold CTA styles.</done>
</task>

<task type="auto">
  <name>task 2: refine shared brand snippets</name>
  <files>snippets/nuumi-icons.liquid, snippets/nuumi-rating-stars.liquid</files>
  <action>
    Standardize the core brand snippets for use in modular sections.
    - Update `snippets/nuumi-icons.liquid` to include all required SVGs: 'paw', 'science', 'shield', 'check_mark', 'star', 'truck', 'return', 'leaf', 'heart'. Ensure they are responsive and use `currentColor`.
    - Refine `snippets/nuumi-rating-stars.liquid` to accept a `rating` (number) and `count` (number) and render a consistent star row using the 'star' icon from `nuumi-icons`.
  </action>
  <verify>
    <automated>grep -q "check_mark" snippets/nuumi-icons.liquid && grep -q "nuumi-icons" snippets/nuumi-rating-stars.liquid</automated>
  </verify>
  <done>Brand snippets standardized and ready for modular section integration.</done>
</task>

<task type="auto">
  <name>task 3: setup Cypress E2E framework</name>
  <files>package.json, cypress.config.js, cypress/e2e/homepage.cy.js</files>
  <action>
    Install and configure Cypress for automated verification of homepage sections.
    - Run `npm install cypress --save-dev`.
    - Create `cypress.config.js` with standard Shopify theme dev server URL (http://127.0.0.1:9292).
    - Create `cypress/e2e/homepage.cy.js` with a basic test that visits the homepage and checks for the presence of the hero section.
  </action>
  <verify>
    <automated>test -f cypress/e2e/homepage.cy.js</automated>
  </verify>
  <done>Cypress installed and configured with initial homepage test suite.</done>
</task>

</tasks>

<verification>
Check for updated nuumi-base.css and existence of brand snippets. Run `npx cypress run` (if a local server is active) or verify file existence.
</verification>

<success_criteria>
- nuumi-base.css contains the premium gold button styles.
- nuumi-icons.liquid includes the full set of brand SVGs.
- Cypress is ready to run homepage tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-homepage-sections/02-01-SUMMARY.md`
</output>
