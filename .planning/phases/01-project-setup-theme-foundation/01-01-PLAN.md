---
phase: 01-project-setup-theme-foundation
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - "nuumipet-v1/"
autonomous: false
requirements:
  - THEME-03
  - THEME-04
must_haves:
  truths:
    - "The Shopify CLI can connect to the specified store."
    - "A new theme based on Dawn is created locally."
    - "The local development server runs successfully."
  artifacts:
    - path: "nuumipet-v1/shopify.theme.toml"
      provides: "Theme configuration file confirming initialization."
      contains: 'name = "Nuumipet V1"'
  key_links:
    - from: "Local machine"
      to: "evsedm-j1.myshopify.com"
      via: "Shopify CLI"
      pattern: "shopify login --store evsedm-j1.myshopify.com"
---

<objective>
Initialize a new Shopify theme using the latest Dawn version and connect it to the target Shopify store for local development.

Purpose: To establish the local project foundation, ensuring the development environment is correctly set up before any code changes are made.
Output: A new local Shopify theme directory named "Nuumipet V1" and a running local development server.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-setup-theme-foundation/01-CONTEXT.md
@.planning/phases/01-project-setup-theme-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Authenticate and Initialize Theme</name>
  <files></files>
  <action>
Use the Shopify CLI to initialize a new theme. This involves two steps:
1.  Log in to the specified Shopify store: `evsedm-j1.myshopify.com`.
2.  Initialize a new theme named "Nuumipet V1" using the default Dawn theme template. This will create a new directory named `nuumipet-v1` in the project root.
</action>
  <verify>
    <automated>shopify login --store evsedm-j1.myshopify.com && shopify theme init "Nuumipet V1"</automated>
  </verify>
  <done>
A new directory `nuumipet-v1` exists and is populated with the Shopify Dawn theme files.
</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>task 2: Verify Local Development Server</name>
  <what-built>A new Shopify theme directory has been created. This task verifies that the theme can be served locally by the Shopify CLI.</what-built>
  <how-to-verify>
1.  Navigate into the newly created theme directory: `cd nuumipet-v1`
2.  Run the command: `shopify theme dev`
3.  The command should successfully execute, open a new browser tab, and display the default Dawn theme preview. You should see a message in your terminal like `┃ Serving... │ http://127.0.0.1:9292`.
4.  Confirm that you can see the live preview in your browser.
</how-to-verify>
  <resume-signal>Type "approved" if the local server runs and you can see the theme preview. If it fails, provide the error message.</resume-signal>
</task>

</tasks>

<verification>
- The command `ls nuumipet-v1` shows a list of standard Shopify theme directories (assets, config, layout, etc.).
- The `shopify theme dev` command runs without authentication or theme connection errors.
</verification>

<success_criteria>
- A developer can successfully run `shopify theme dev` within the `nuumipet-v1` directory and view a live preview of the Dawn theme.
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup-theme-foundation/01-01-SUMMARY.md`
</output>
