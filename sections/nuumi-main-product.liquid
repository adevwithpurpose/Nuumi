<product-info
  id="MainProduct-{{ section.id }}"
  class="nuumi-pdp section-{{ section.id }}-padding"
  data-section="{{ section.id }}"
  data-product-id="{{ product.id }}"
  data-update-url="true"
  data-url="{{ product.url }}"
>
  {{ 'section-main-product.css' | asset_url | stylesheet_tag }}
  {{ 'component-price.css' | asset_url | stylesheet_tag }}
  {{ 'component-deferred-media.css' | asset_url | stylesheet_tag }}
  {{ 'nuumi-pdp.css' | asset_url | stylesheet_tag }}

  <script src="{{ 'product-info.js' | asset_url }}" defer="defer"></script>
  <script src="{{ 'media-gallery.js' | asset_url }}" defer="defer"></script>
  <script src="{{ 'product-form.js' | asset_url }}" defer="defer"></script>

  {% assign variant_images = product.images | where: 'attached_to_variant?', true | map: 'src' %}

  <div class="page-width">
    <div class="nuumi-pdp__grid">
      <!-- Media Gallery -->
      {% render 'nuumi-hero-gallery',
        variant_images: variant_images,
        section: section,
        shell_class: 'nuumi-pdp__media-wrapper',
        media_column_class: '',
        show_left_guarantee: false,
        left_guarantee_class_prefix: 'nuumi-pdp',
        hide_variants: section.settings.hide_variants
      %}

      <!-- Product Info -->
      <div class="nuumi-pdp__info-wrapper">
        <section id="ProductInfo-{{ section.id }}" class="nuumi-pdp__info-container">
          {%- assign product_form_id = 'product-form-' | append: section.id -%}

          {%- for block in section.blocks -%}
            {%- case block.type -%}
              {%- when 'title' -%}
                <div class="nuumi-pdp__title-block" {{ block.shopify_attributes }}>
                  {% assign hero_eyebrow = product.metafields.custom.hero_eyebrow.value | default: product.metafields.custom.eyebrow.value | default: block.settings.eyebrow %}
                  {% assign hero_badge_primary = product.metafields.custom.badge_primary.value | default: block.settings.badge_primary %}
                  {% assign hero_badge_secondary = product.metafields.custom.badge_secondary.value | default: block.settings.badge_secondary %}
                  {% assign hero_subtitle = product.metafields.custom.subtitle.value | default: product.metafields.descriptors.subtitle.value | default: block.settings.subtitle %}
                  {% assign hero_description = product.metafields.custom.hero_description.value | default: product.description | default: block.settings.description %}

                  {%- if hero_eyebrow != blank -%}
                    <p class="nuumi-pdp__eyebrow">{{ hero_eyebrow }}</p>
                  {%- endif -%}
                  {%- if hero_badge_primary != blank or hero_badge_secondary != blank -%}
                    <div class="nuumi-pdp__badges">
                      {%- if hero_badge_primary != blank -%}
                        <span class="nuumi-pdp__badge nuumi-pdp__badge--primary">{{ hero_badge_primary }}</span>
                      {%- endif -%}
                      {%- if hero_badge_secondary != blank -%}
                        <span class="nuumi-pdp__badge nuumi-pdp__badge--secondary">{{ hero_badge_secondary }}</span>
                      {%- endif -%}
                    </div>
                  {%- endif -%}
                  <h1 class="nuumi-pdp__title">{{ product.title | escape }}</h1>
                  {%- if hero_subtitle != blank -%}
                    <p class="nuumi-pdp__subtitle">{{ hero_subtitle }}</p>
                  {%- endif -%}
                  {%- if hero_description != blank -%}
                    <div class="nuumi-pdp__description rte">{{ hero_description }}</div>
                  {%- endif -%}
                </div>

              {%- when 'price' -%}
                <div class="nuumi-pdp__price-block" id="price-{{ section.id }}" role="status" {{ block.shopify_attributes }}>
                  {%- render 'price', product: product, use_variant: true, show_badges: true, price_class: 'price--large' -%}
                </div>

              {%- when 'bullets' -%}
                {% assign hero_bullets = product.metafields.custom.hero_bullets.value %}
                {% assign fallback_bullets = block.settings.bullet_1 | append: block.settings.bullet_2 | append: block.settings.bullet_3 %}
                {%- if hero_bullets != blank or fallback_bullets != blank -%}
                  <ul class="nuumi-pdp__bullets" {{ block.shopify_attributes }}>
                    {%- if hero_bullets != blank -%}
                      {%- for bullet in hero_bullets -%}
                        {%- if bullet != blank -%}
                          <li>
                            <span class="nuumi-pdp__bullet-icon">{% render 'nuumi-icons', icon: 'check_mark' %}</span>
                            {{ bullet }}
                          </li>
                        {%- endif -%}
                      {%- endfor -%}
                    {%- else -%}
                      {%- if block.settings.bullet_1 != blank -%}
                        <li>
                          <span class="nuumi-pdp__bullet-icon">{% render 'nuumi-icons', icon: 'check_mark' %}</span>
                          {{ block.settings.bullet_1 }}
                        </li>
                      {%- endif -%}
                      {%- if block.settings.bullet_2 != blank -%}
                        <li>
                          <span class="nuumi-pdp__bullet-icon">{% render 'nuumi-icons', icon: 'check_mark' %}</span>
                          {{ block.settings.bullet_2 }}
                        </li>
                      {%- endif -%}
                      {%- if block.settings.bullet_3 != blank -%}
                        <li>
                          <span class="nuumi-pdp__bullet-icon">{% render 'nuumi-icons', icon: 'check_mark' %}</span>
                          {{ block.settings.bullet_3 }}
                        </li>
                      {%- endif -%}
                    {%- endif -%}
                  </ul>
                {%- endif -%}

              {%- when 'quantity_packs' -%}
                <div class="nuumi-pdp__quantity-packs" {{ block.shopify_attributes }}>
                  <p class="nuumi-pdp__label">{{ block.settings.heading }}</p>
                  <div class="nuumi-pdp__pack-grid">
                    <label class="nuumi-pdp__pack-option nuumi-pdp__pack-option--selected">
                      <input type="radio" name="quantity_pack" value="1" checked>
                      <div class="nuumi-pdp__pack-content">
                        <span class="nuumi-pdp__pack-title">{{ block.settings.pack_1_title }}</span>
                        <span class="nuumi-pdp__pack-subtitle">{{ block.settings.pack_1_subtitle }}</span>
                      </div>
                    </label>
                    <label class="nuumi-pdp__pack-option nuumi-pdp__pack-option--popular">
                      {%- if block.settings.pack_2_badge != blank -%}
                        <div class="nuumi-pdp__pack-badge">{{ block.settings.pack_2_badge }}</div>
                      {%- endif -%}
                      <input type="radio" name="quantity_pack" value="3">
                      <div class="nuumi-pdp__pack-content">
                        <span class="nuumi-pdp__pack-title">{{ block.settings.pack_2_title }}</span>
                        <span class="nuumi-pdp__pack-subtitle">{{ block.settings.pack_2_subtitle }}</span>
                      </div>
                    </label>
                    <label class="nuumi-pdp__pack-option">
                      <input type="radio" name="quantity_pack" value="6">
                      <div class="nuumi-pdp__pack-content">
                        <span class="nuumi-pdp__pack-title">{{ block.settings.pack_3_title }}</span>
                        <span class="nuumi-pdp__pack-subtitle">{{ block.settings.pack_3_subtitle }}</span>
                      </div>
                    </label>
                  </div>
                  <!-- Hidden actual quantity input for Dawn's form -->
                  <input type="hidden" name="quantity" value="1" form="{{ product_form_id }}" id="Quantity-{{ section.id }}">
                </div>

              {%- when 'buy_buttons' -%}
                {% assign cta_label = product.metafields.custom.cta_label.value | default: block.settings.button_label %}
                {% assign guarantee_text = product.metafields.custom.guarantee_copy.value | default: block.settings.guarantee_text %}
                {% assign cta_note = product.metafields.custom.cta_note.value | default: block.settings.cta_note %}
                {% assign shipping_note = product.metafields.custom.shipping_note.value | default: block.settings.shipping_note %}
                {% assign guarantee_note = product.metafields.custom.guarantee_note.value | default: block.settings.guarantee_note %}
                {% assign payment_note = product.metafields.custom.payment_note.value | default: block.settings.payment_note %}

                <div id="nuumi-buy-box" class="nuumi-pdp__buy-buttons" {{ block.shopify_attributes }}>
                  <product-form class="product-form" data-section-id="{{ section.id }}">
                    <div class="product-form__error-message-wrapper" role="alert" hidden>
                      <span class="svg-wrapper">
                        {{- 'icon-error.svg' | inline_asset_content -}}
                      </span>
                      <span class="product-form__error-message"></span>
                    </div>

                    {%- form 'product', product, id: product_form_id, class: 'form', novalidate: 'novalidate', data-type: 'add-to-cart-form' -%}
                      <input type="hidden" name="id" value="{{ product.selected_or_first_available_variant.id }}" class="product-variant-id">
                      <div class="product-form__buttons">
                        <button
                          type="submit"
                          name="add"
                          class="nuumi-button--gold product-form__submit button--full-width"
                          {% if product.selected_or_first_available_variant.available == false %}disabled{% endif %}
                        >
                            <span>
                              {%- if product.selected_or_first_available_variant.available == false -%}
                                {{ 'products.product.sold_out' | t }}
                              {%- else -%}
                                {{ cta_label }} — {{ product.price | money }}
                              {%- endif -%}
                            </span>
                          {%- render 'loading-spinner' -%}
                        </button>
                      </div>
                    {%- endform -%}
                  </product-form>
                </div>
                
                {%- if block.settings.show_guarantee and guarantee_text != blank -%}
                  <div class="nuumi-pdp__guarantee">
                    <span class="nuumi-pdp__guarantee-icon">{% render 'nuumi-icons', icon: 'shield' %}</span>
                    <span>{{ guarantee_text }}</span>
                  </div>
                {%- endif -%}

                {%- if cta_note != blank -%}
                  <p class="nuumi-pdp__cta-note">{{ cta_note }}</p>
                {%- endif -%}

                {%- if shipping_note != blank or guarantee_note != blank -%}
                  <div class="nuumi-pdp__micro-trust">
                    {%- if shipping_note != blank -%}
                      <span>{{ shipping_note }}</span>
                    {%- endif -%}
                    {%- if guarantee_note != blank -%}
                      <span>{{ guarantee_note }}</span>
                    {%- endif -%}
                  </div>
                {%- endif -%}

                {%- if payment_note != blank -%}
                  <p class="nuumi-pdp__payment-note">{{ payment_note }}</p>
                {%- endif -%}

            {%- endcase -%}
          {%- endfor -%}
        </section>
      </div>
    </div>
  </div>
</product-info>

<!-- Sticky Mobile CTA -->
{%- if section.settings.enable_sticky_cta -%}
  <div class="nuumi-sticky-cta">
    <div class="nuumi-sticky-cta__inner page-width">
      <div class="nuumi-sticky-cta__info">
        <span class="nuumi-sticky-cta__title">{{ product.title }}</span>
        <span class="nuumi-sticky-cta__price">{{ product.price | money }}</span>
      </div>
      <button type="button" class="nuumi-button--gold nuumi-sticky-cta__button" onclick="document.querySelector('#ProductSubmitButton-{{ section.id }}, .nuumi-pdp__buy-buttons button[type=\'submit\']').click();">
        Add to Cart
      </button>
    </div>
  </div>
{%- endif -%}

<script>
  document.addEventListener('DOMContentLoaded', function() {
    // Quantity Pack Logic
    const packRadios = document.querySelectorAll('input[name="quantity_pack"]');
    const hiddenQuantityInput = document.getElementById('Quantity-{{ section.id }}');
    
    packRadios.forEach(radio => {
      radio.addEventListener('change', function() {
        if (this.checked) {
          hiddenQuantityInput.value = this.value;
          
          // Update styling
          document.querySelectorAll('.nuumi-pdp__pack-option').forEach(opt => opt.classList.remove('nuumi-pdp__pack-option--selected'));
          this.closest('.nuumi-pdp__pack-option').classList.add('nuumi-pdp__pack-option--selected');
        }
      });
    });
    // Sticky CTA Logic
    const stickyCta = document.querySelector('.nuumi-sticky-cta');
    const buyButtons = document.querySelector('.nuumi-pdp__buy-buttons');
    
    if (stickyCta && buyButtons) {
      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (!entry.isIntersecting && entry.boundingClientRect.top < 0) {
            stickyCta.classList.add('nuumi-sticky-cta--visible');
          } else {
            stickyCta.classList.remove('nuumi-sticky-cta--visible');
          }
        });
      }, { threshold: 0 });
      
      observer.observe(buyButtons);
    }
  });
</script>

{% schema %}
{
  "name": "Nuumi Main Product",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "checkbox",
      "id": "enable_sticky_cta",
      "label": "Enable mobile sticky CTA",
      "default": true
    },
    {
      "type": "header",
      "content": "Media settings"
    },
    {
      "type": "select",
      "id": "media_size",
      "options": [
        {
          "value": "small",
          "label": "Small"
        },
        {
          "value": "medium",
          "label": "Medium"
        },
        {
          "value": "large",
          "label": "Large"
        }
      ],
      "default": "large",
      "label": "Media size"
    },
    {
      "type": "checkbox",
      "id": "constrain_to_viewport",
      "default": true,
      "label": "Constrain media to viewport"
    },
    {
      "type": "select",
      "id": "media_fit",
      "options": [
        {
          "value": "contain",
          "label": "Contain"
        },
        {
          "value": "cover",
          "label": "Cover"
        }
      ],
      "default": "contain",
      "label": "Media fit"
    },
    {
      "type": "select",
      "id": "gallery_layout",
      "options": [
        {
          "value": "stacked",
          "label": "Stacked"
        },
        {
          "value": "columns",
          "label": "Columns"
        },
        {
          "value": "thumbnail",
          "label": "Thumbnail"
        },
        {
          "value": "thumbnail_slider",
          "label": "Thumbnail slider"
        }
      ],
      "default": "stacked",
      "label": "Desktop layout"
    },
    {
      "type": "select",
      "id": "mobile_thumbnails",
      "options": [
        {
          "value": "columns",
          "label": "Columns"
        },
        {
          "value": "show",
          "label": "Show thumbnails"
        },
        {
          "value": "hide",
          "label": "Hide thumbnails"
        }
      ],
      "default": "hide",
      "label": "Mobile thumbnails"
    },
    {
      "type": "checkbox",
      "id": "enable_video_looping",
      "default": false,
      "label": "Enable video looping"
    },
    {
      "type": "checkbox",
      "id": "hide_variants",
      "default": false,
      "label": "Hide variant media"
    }
  ],
  "blocks": [
    {
      "type": "title",
      "name": "Title & Subtitle",
      "limit": 1,
      "settings": [
        {
          "type": "text",
          "id": "subtitle",
          "label": "Subtitle",
          "default": "Veterinarian Formulated"
        },
        {
          "type": "text",
          "id": "eyebrow",
          "label": "Eyebrow",
          "default": "Daily support for modern pet routines"
        },
        {
          "type": "text",
          "id": "badge_primary",
          "label": "Primary badge",
          "default": "Best Seller"
        },
        {
          "type": "text",
          "id": "badge_secondary",
          "label": "Secondary badge",
          "default": "Premium Support"
        },
        {
          "type": "richtext",
          "id": "description",
          "label": "Description",
          "default": "<p>Built for pet parents who want a more intentional routine with clearer benefits and easier daily use.</p>"
        }
      ]
    },
    {
      "type": "price",
      "name": "Price",
      "limit": 1
    },
    {
      "type": "bullets",
      "name": "Benefit Bullets",
      "limit": 1,
      "settings": [
        {
          "type": "text",
          "id": "bullet_1",
          "label": "Bullet 1",
          "default": "Clinically proven ingredients"
        },
        {
          "type": "text",
          "id": "bullet_2",
          "label": "Bullet 2",
          "default": "Made in the USA"
        },
        {
          "type": "text",
          "id": "bullet_3",
          "label": "Bullet 3",
          "default": "100% Satisfaction Guarantee"
        }
      ]
    },
    {
      "type": "quantity_packs",
      "name": "Quantity Packs",
      "limit": 1,
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "label": "Heading",
          "default": "Choose your supply"
        },
        {
          "type": "text",
          "id": "pack_1_title",
          "label": "Pack 1 title",
          "default": "1 Pack"
        },
        {
          "type": "text",
          "id": "pack_1_subtitle",
          "label": "Pack 1 subtitle",
          "default": "30 Day Supply"
        },
        {
          "type": "text",
          "id": "pack_2_title",
          "label": "Pack 2 title",
          "default": "3 Packs"
        },
        {
          "type": "text",
          "id": "pack_2_subtitle",
          "label": "Pack 2 subtitle",
          "default": "90 Day Supply"
        },
        {
          "type": "text",
          "id": "pack_2_badge",
          "label": "Pack 2 badge",
          "default": "Most Popular"
        },
        {
          "type": "text",
          "id": "pack_3_title",
          "label": "Pack 3 title",
          "default": "6 Packs"
        },
        {
          "type": "text",
          "id": "pack_3_subtitle",
          "label": "Pack 3 subtitle",
          "default": "180 Day Supply"
        }
      ]
    },
    {
      "type": "buy_buttons",
      "name": "Buy Buttons",
      "limit": 1,
      "settings": [
        {
          "type": "checkbox",
          "id": "show_guarantee",
          "label": "Show Guarantee",
          "default": true
        },
        {
          "type": "text",
          "id": "guarantee_text",
          "label": "Guarantee Text",
          "default": "90-Day Money Back Guarantee"
        },
        {
          "type": "text",
          "id": "button_label",
          "label": "Button Label",
          "default": "Add to Cart"
        },
        {
          "type": "text",
          "id": "cta_note",
          "label": "CTA Note",
          "default": "Built for a simple, repeatable daily routine."
        },
        {
          "type": "text",
          "id": "shipping_note",
          "label": "Shipping Note",
          "default": "Fast US shipping"
        },
        {
          "type": "text",
          "id": "guarantee_note",
          "label": "Guarantee Note",
          "default": "Satisfaction support available"
        },
        {
          "type": "text",
          "id": "payment_note",
          "label": "Payment Note",
          "default": "Secure checkout with major payment methods"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Nuumi Main Product"
    }
  ]
}
{% endschema %}
